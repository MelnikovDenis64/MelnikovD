[pytest]

markers =
    smoke
    regress
    test_1
    test_2
    test_3
    feature_test: pet project test

addopts =
     -sv
    --alluredir=allure-results
     --clean-alluredir
    --reruns=2
    --only-rerun TimeoutException
    --only-rerun HTTPSConnectionPool
    --only-rerun StaleElementReferenceException
    --only-rerun InvalidSessionIdException


